{
  "name": "VirgilHR-Automation-Framework",
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.0",
  "description": "End-to-End test automation for VirgilHR",
  "main": "index.js",
  "scripts": {
    "install:browsers": "pnpm exec playwright install --with-deps",
    "test:playwright": "pnpm exec playwright test",
    "test:playwright:api": "pnpm exec playwright test tests/API",
    "test:regression:api": "pnpm exec playwright test --grep @regression_API",
    "test:regression:ui": "pnpm exec playwright test --project=chromium --workers=1 --headed",
    "test:regression:ui:customer-management": "pnpm exec playwright test --project=chromium --workers=1 --headed --grep @customer_management",
    "test:regression:ui:partner-management": "pnpm exec playwright test --project=chromium --workers=1 --headed --grep @partner_management",
    "test:regression:ui:member-portal": "pnpm exec playwright test --project=chromium --workers=1 --headed --grep @member_portal",
    "test:regression:ui:partner-portal": "pnpm exec playwright test --project=chromium --workers=1 --headed --grep @partner_portal",
    "test:playwright:chromium": "pnpm exec playwright test --project=chromium",
    "playwright:report": "pnpm exec playwright show-report",
    "playwright:codegen": "pnpm exec playwright codegen",
    "lint": "pnpm exec tsc --noEmit --pretty"
  },
  "scriptDescriptions": {
    "install:browsers": "Install Playwright browser binaries and system dependencies (run after install or when Playwright is upgraded).",
    "test:playwright": "Run the full Playwright suite under tests/ with ts-node and tsconfig-paths so src/* imports resolve.",
    "test:playwright:api": "Run only API specs under tests/API with the same TypeScript path setup as test:playwright.",
    "test:regression:api": "Run the full API regression suite (tests tagged @regression_API).",
    "test:regression:ui": "Run the full UI regression suite (tests tagged @regression_UI) on Chromium.",
    "test:regression:ui:customer-management": "Run the customer management UI regression suite (tests tagged @customer_management) on Chromium.",
    "test:regression:ui:partner-management": "Run the partner management UI regression suite (tests tagged @partner_management) on Chromium.",
    "test:regression:ui:member-portal": "Run the member portal UI regression suite (tests tagged @member_portal) on Chromium.",
    "test:regression:ui:partner-portal": "Run the partner portal UI regression suite (tests tagged @partner_portal) on Chromium.",
    "test:playwright:chromium": "Run Playwright tests for the chromium project only (faster local/CI smoke).",
    "playwright:report": "Open the last HTML test report (run after a test run that produced playwright-report/).",
    "playwright:codegen": "Launch Playwright codegen to record locators and flows against a URL.",
    "lint": "Type-check the project with tsc --noEmit (no emit); fails on TS errors in src, tests, and playwright.config.ts."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.15.21",
    "cross-env": "^7.0.3",
    "playwright": "^1.61.1",
    "pnpm": "^10.33.0",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^3.14.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@faker-js/faker": "^10.1.0",
    "date-fns": "^4.1.0",
    "dotenv": "^16.5.0",
    "easy-yopmail": "^5.2.2",
    "mailslurp-client": "^17.2.0",
    "simple-maildrop": "^0.2.0",
    "uuid": "^13.0.0"
  }
}
